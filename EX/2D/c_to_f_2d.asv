function  r_h = c_to_f_2d(r_2h,nx)
   n = size(r_2h,1);
   r_h = zeros(n*2+1,1);
    
   nx_h = 2*nx;
   
   for i = 1:nx
       ii = 2*i;
        for j = 1:nx
            jj = 2*j;
            
            r_h( ii*nx_h + jj) = r_h( ii*nx_h + jj) + 
        end
   end
   
end

